{% extends 'base.html.twig' %}

{% block title %}Aloitelista{% endblock %}

 {% block body %}

    <h1>Aloitteen tiedot</h1>
    <table class="table table-hover">
    <thead>
        <tr>
        <th>Aihe</th>
        <th>Kuvaus</th>
        <th>Kirjauspäivä</th>
        <th>Toiminnot</th>
        </tr>
    </thead>
    <tbody>
        <tr>
        <td>{{ aloite.aihe }}</td>
        <td>{{ aloite.kuvaus }}</td>
        <td>{{ aloite.kirjauspaiva | date('d-m-Y') }}</td>

        <td>
            <a href="{{ path('aloite_naytakaikki', {id: aloite.id}) }}" class="btn btn-success btn-sm">Näytä kaikki tiedot</a>
        </td>
        </tr>               
    </tbody>    
</table> 
 
 {% endblock %}

 {% block javascripts %}
    {{ parent() }}
    <script src="js/main.js"></script>
{% endblock %}